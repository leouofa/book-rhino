=r ux.container
  =r ux.row
    =r ux.column width: 16
      =r ux.h2 'center aligned !font-mono'
        | Writing Styles
        =r ux.label text: @total_writing_styles, class: 'circular !bg-black !text-white'

    =r ux.div 'text-center'
      = paginate @writing_styles

    - @writing_styles.each do |tweet|
      = render 'tweet', tweet: tweet

    =r ux.div 'text-center'
      = paginate @writing_styles
